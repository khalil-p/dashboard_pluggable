import { createContext } from "react";

// export const zoomContext = createContext({isOpen:false,setIsOpen:function(){}});
export const zoomContext = createContext({});
export const currentStatusInfoContext = createContext({})